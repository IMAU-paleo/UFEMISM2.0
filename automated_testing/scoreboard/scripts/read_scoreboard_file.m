function single_run = read_scoreboard_file( filename)
% Read a structure containing the results of all the runs of a
% component/integrated test from a scoreboard file

single_run = readstruct( filename);

end